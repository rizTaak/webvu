# webvu

## code change instructions
1. code changes are not done directly, instead SPEC.md is updated with specs and then code is updated accordingly
2. all code changes should have unit and integration tests
3. backward compatibility is very important and should be maintain for user configured things
4. all user inteface needs to be responsive
5. all code should be properly structured, it should follow standard naming conventions, should be well designed and maintainable, avoid duplication and prefer refactor
